# 小车端验收记录

日期：2026-09-14。全部文件独立于平台源码。

## 已验证

- React/TypeScript/Vite 生产构建通过。
- 后端鉴权、跨源拒绝、速度范围、地图路径校验、障碍/未知区拒绝、旋转地图坐标转换、地图保存读取、幂等保存、WS 消息等测试通过。
- 小车实机运行 systemd 服务，读取真实速度、IMU、雷达频率、CPU、内存、温度和电压。
- 摄像头 Astra RGB 已显示真实 640×480 图像。
- RViz 在独立 Xvfb 显示运行，避免桌面推流递归；已处理 OpenCV 污染 Qt 插件路径的问题。
- 保存“现场地图 0914”，建图转巡航前自动保存备份。
- Nav2 地图加载和人工定位通过；静止雷达匹配约 98–99%；真实 ComputePathToPose 返回 14 个路径点。界面拖动选点后实际路径预览通过。该比例不是绝对定位准确率。
- 自动全局定位已接入，现场静止时未在等待窗口内可靠收敛，人工初始化后正常。
- 导航取消：正常取消、迟到接受、取消失败不误报暂停三项测试在小车 ROS 环境通过。
- 摄像头和 RViz 双路 RTMP 向小车回环地址的临时接收器推流成功，ffprobe 验证 H.264，尺寸分别为 640×480 和 1100×650。测试后恢复为空目标，没有发送到外部平台。
- 平台注册和遥测消息通过本地模拟接收器验证；未配置真实平台目标。
- 浏览器验证 1024×768 和 1280×720 布局、保存地图、巡航切换、拖动航点和真实路线预览。
- 根据追加需求，现场视频改为原始宽高比、object-fit:contain；缩小 IMU 高度，建图按钮更名“重置地图”。新版静态页面已同步到小车。

## 部署验证与边界

- 电池/底盘订阅及端点已部署实测通过：电压约 10.89 V，/odom 里程计与底盘在线状态正常；充电数据与电量百分比未发布，返回 null。
- 20:40 整机重启验证通过：自动联网，systemd 服务 active，专用 Firefox --kiosk 自动启动，健康接口正常。启动模式 idle，未自动发出运动目标。
- 新增电池百分比仅接标准 BatteryState，不从电压估算；没有 BMS 百分比时显示电压。
- 真实场地动态导航、避障、多点循环尚未作为受控测试执行。用户现场操作导致状态变化不能替代本次测试证据。

## 设计复核

布局采用深色双页控制台、主地图与右侧摄像头/状态；使用用户实际 logo。功能字词保持简短。实际地图与视频均来自 ROS/设备，不使用概念图假数据。窄屏右侧允许滚动，停止按钮固定在顶部。演示后端仅用于布局验证，明确标记“演示”。

追加验证：小车 ROS 环境 15 项测试全部通过（含电压非法值拒绝、BMS 百分比合法性）；IMU 曲线区域固定高度，1280×720 下完整显示。重启前已保存“部署重启前备份”。

## 真实屏幕修正

用户照片暴露先前验证高度不符。通过小车 xrandr 确认 HDMI-1 实际为 **1024×600**，先前 1024×768 不能作为此屏验收依据。已添加高度≤650px 的布局：取消 620px 页面最低高度，IMU 放入地图下方，与状态摘要并排；视频保持源比例。生产构建和静态部署完成，不重启 ROS。

浏览器按 1024×600 验证：document.scrollWidth=1024，scrollHeight=600；可见 IMU 图表高度46px、bottom=557.5px；摄像头259×194.25px（4:3），图像完整，无页面纵向溢出。该4:3只代表摄像头图像，不代表屏幕比例。

## 布局重做（1024×600 实屏驱动）

日期：2026-09-14 第二次现场修改。触发问题：实时地图四周大片空白（RViz 画面被窗口边框和 10×10 m 网格包围）、IMU 三条曲线被压成一条直线、巡航页的“点到点/多点/循环/开始/暂停”被挤到屏幕外。

根因（实机取证，非推断）：

- Xvfb 为 1100×450，RViz 窗口占满其中，真正的 3D 渲染区只有 1050×341：菜单栏 71px、状态栏 39px、左右各 25px 边框全部被一起推流，再被网页按比例缩进面板，形成“蓝边”。
- RViz 视图 Scale=32、Grid `Plane Cell Count: 10`，可见范围约 33×11 m，网格只占中间一小块，其余是空背景；待命时底盘不发布 `map` 帧，固定坐标系 `map` 不存在，网格与车体都渲染不出来。
- 工作区 `grid-template-rows` 为隐式 `auto`，右栏内容（摄像头 + IMU + 设备状态）把行撑高，设备状态面板溢出到页脚；巡航任务面板同样被裁切。

修改：

- 前端按 1024×600 重排：顶栏 46、页脚 22、工作区留白 8、右栏 292。地图面板 708×472，地图可视区 **694×434**；摄像头 278×208.5（源比例 4:3）。左栏工具条内联运行指标，右栏自上而下为现场视频、IMU（图例 + 三条曲线）、设备状态。
- IMU 改为三条独立泳道，各自按窗口内幅度归一化（Roll/Pitch 最小量程 0.6°、Yaw 1.5°），静止时不再被固定量程钳成直线；数值与曲线同色。
- 巡航任务面板压缩为单行航点行 + 单行速度条 + “预览/开始巡航/暂停”同一行，面板内部滚动兜底；工作区显式 `grid-template-rows: minmax(0,1fr)`，右栏 `overflow: hidden`，任何情况下不再溢出页脚。
- 后端 `media.py`：Xvfb 屏幕 = 推流尺寸 694×434，`rviz-layout.sh` 带参数把 RViz 窗口移到 (-25,-71) 并设 744×544，截屏区域恰好只剩 3D 渲染区；Xvfb 加 `-nocursor`。`console.rviz` 每次启动复制到 `runtime/rviz/` 后再加载，避免 RViz 回写污染权威配置。
- `console.rviz`：背景改为与面板一致的 `13;23;40`；固定坐标系改 `odom_combined`（待命时唯一始终存在的世界系）；1 m 细网格 40 格 + 5 m 主网格，Scale=30（约 23×14.5 m）；去掉工具光标。
- `ros_bridge.py` 每 0.2 s 发布 `odom_combined → console_view`（只平移、不旋转），RViz 视角以它为目标帧，小车居中且地图朝上。

现场验证（ffmpeg 抓取真实 HDMI 画面 + 推流取样）：

- 1024×600 无滚动、无溢出：本地与实机截图均为 1024×600，页脚完整。
- 实时推流实测 **694×434**，四角像素为背景色 `(13,23,40)`，无菜单栏/状态栏残留；网格铺满整幅画面，待命时不再有大片空白。
- 建图实测：雷达 10.1 Hz、地图 800×800、分辨率 0.05 m，RViz 画面显示真实占据栅格与激光点，铺满面板。
- 巡航实测：加载“切换前备份 09-14 21:09”后 AMCL 收敛（已定位），巡航任务面板中点到点/多点/循环、新建路线、添加航点、巡航速度、预览/开始巡航/暂停全部在首屏可见，页脚未被覆盖。
- 摄像头 278×208.5 = 4:3，与 640×480 源一致，`object-fit: contain` 不拉伸。
- `python3 -m unittest discover -s tests` 在小车 ROS 环境 **15 项全部通过**；生产前端与小车上 `dist` 资源 md5 一致。

边界与未验证：

- 未在真实场地执行动态导航、避障和循环巡航；本次只验证页面、推流与建图/加载状态，未下发运动目标。
- `console_view` 跟随逻辑在静止状态下验证（坐标系存在、视角正常）；实际行驶中的跟随效果需现场确认。
- RViz 启动后会有数秒自带的启动画面，属于进程启动瞬态。
- 验证用摄像头画面偏暗是现场光照所致，非显示缺陷。

## 人工定位吸附微调

日期：2026-09-14 第三次现场修改。需求：手动定位点下去总差一点点，系统不会自动吸附微调。

设计（`core.py`，与 ROS 无关，可离线测试）：

- **移出障碍**：点选落在障碍或未知栅格时，向外螺旋找到最近可通行栅格中心（上限 3 m）。
- **匹配微调**：把最近一帧雷达点（≤240 个，转到 `base_footprint`）按候选位姿投到地图上，
  用“离障碍物越近分数越高”的高斯似然表（半径 0.25 m、σ=0.12 m，按地图版本缓存）打分，
  在 ±0.35 m / ±12° 内以 5 cm / 2° 步长搜索最优位姿。
- **安全判据**：只有提升 ≥0.06 且 ≥15%，并且（结果吻合度 ≥0.35 或提升 ≥0.20）才移动，
  否则保持用户点选位置并返回 `reason=weak_match`。候选位姿还必须落在可通行栅格内
  （否则会被地图校验以 422 拒绝）。环境与地图不符时宁可不吸附。
- **性能**：候选位姿整批向量化（每个偏航角一次算完所有平移），Jetson 实测 800×800 地图
  似然表 63 ms、搜索 81 ms（未向量化时同场景要 10 s）；接口侧再放进线程执行，不阻塞状态推流。
- **手动微调**：地图右下角新增 6 键微调盘（↺↑↻ / ←↓→），平面 2 cm、旋转 1.5° 一档，
  请求带 `snap:false` 直接发布，避免被自动吸附拉回。

验证：

- `tests/test_snap.py` 用合成房间 + 射线投射的假雷达：0.17 m / 6° 的偏差被吸附到 0.01 m / 1°，
  吻合度 0.356 → 0.94；随机噪声扫描时判据不足、保持原位；点在墙上会移出障碍。
- 小车 ROS 环境 22 项单元测试全部通过（含新增 6 项吸附测试）。
- 实机接口实测：地图 800×800 时单次吸附 0.38–1.41 s（含负载排队），返回
  `pose / shift_m / shift_deg / score / base_score / samples / reason`；页面上点击定位会弹出
  “定位已提交 · 已吸附微调 …”或“（未吸附：此处吻合度不足）”，工具条常显“定位 待定位 25%”这类吻合度。
- 微调盘实机点击验证：一次点击下发 2 cm 平移，提示“已微调 2 cm”。

边界：

- 吸附是**小范围微调**（±0.35 m / ±12°），不是全局重定位；点得太远请用自动定位或重新点选。
- 实测当时机器人所处位置的雷达与地图本身吻合度只有 16–37%（地图为短时试建，现场与建图时不一致），
  因此实机演示到的是“判据不足、保持点选位置”的分支；在吻合良好的场地吸附会正常生效。
- 微调盘发布的是 `/initialpose`，最终位姿仍由 AMCL 收敛决定；环境吻合差时 AMCL 可能跳到别的局部最优。
