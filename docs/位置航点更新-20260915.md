# 位置航点更新

已去掉航点编辑中的方向字段、侧栏角度、朝向沿路线按钮。航点只含 X/Y，地图选点不再显示朝向拖拽箭头；人工定位保留方向操作。

旧路线历史 yaw 在读取时忽略，新保存的路线只有 X/Y，旧路线文件原件保持备份。内部根据相邻段计算切向，前向规划器自动生成具有曲率约束的避障路径。规划/控制最小转弯半径沿用 0.35m，不擅自降低。没有改变用户保存的航点位置。

验证：43项后台测试通过，覆盖仅位置输入、旧方向忽略、拐角切向、循环闭合、重合点、预览/任务入口；前端构建与30航点弹窗操作测试通过，无朝向字段。未执行实车巡航。

部署前保存了正在运行的建图：航点更新前备份 09-15 14:18，地图ID map_20260915_141841_931f02。部署后控制台重启为待命。

备份目录：/home/wheeltec/mumai-console/runtime/backups/position-waypoints/。
